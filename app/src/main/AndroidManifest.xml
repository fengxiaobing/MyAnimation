<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.myanimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyViewPropertyAnimatorActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- <activity android:name=".ValueAnimatorIntActivity"> -->
        <!-- </activity> -->
        <!-- <activity android:name=".MyObjectAnimatorCircleColorActivity"> -->
        <!-- </activity> -->
        <!-- <activity android:name=".MyObjectAnimatorButtonActivity"> -->
        <!-- </activity> -->
        <!-- <activity android:name=".MyAnimatorSetActivity"> -->
        <!-- </activity> -->
        <!--<activity android:name=".MyViewPropertyAnimatorActivity">-->
        <!--</activity>-->
    </application>

</manifest>